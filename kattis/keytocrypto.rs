use std::io;

// https://open.kattis.com/problems/keytocrypto

fn rotate(letter: char, shift: i32) -> char {
    'a'
}

fn main() {
    let a = String::from("ar");
    let b = a.chars().collect::<Vec<char>>();

    println!("{:?}", b);
}
